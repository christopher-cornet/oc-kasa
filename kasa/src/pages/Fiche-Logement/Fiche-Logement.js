import styles from './Fiche-Logement.module.scss';

function Logement() {
    return (
        <div>
            <h1 className={styles.title}>Logement</h1>
        </div>
    );
}

export default Logement;